CONFIG_ZMK_USB_LOGGING=n

# ZMK Studio locking — requires unlock combo before Studio/KeyPeek can mutate keymap.
# Studio itself is enabled per-build via build.yaml cmake-args on totem_left only.
CONFIG_ZMK_STUDIO_LOCKING=y
